@model List<Food>

<h2>Receptek</h2>

@foreach (var food in Model)
{
        <div>
            <h3>@food.Name</h3>
            <p>@food.Description</p>
            <img src="@food.ImageUrl" alt="Food Image" width="200" height="150" />
        </div>
}
